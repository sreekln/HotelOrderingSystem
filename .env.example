# =====================================================
# DATABASE CONFIGURATION
# Choose ONE of the following database options:
# =====================================================

# Option 1: Azure SQL Database / SQL Server (Default)
DB_SERVER=your-server.database.windows.net
DB_NAME=hotel_ordering_system
DB_USER=your_db_admin_user
DB_PASSWORD=your_db_password

# Option 2: Supabase PostgreSQL (Alternative)
# Uncomment these and comment out the SQL Server config above
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# =====================================================
# APPLICATION CONFIGURATION
# =====================================================

# JWT Configuration (Required for SQL Server option)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# API Configuration
VITE_API_URL=http://localhost:3001/api

# =====================================================
# OPTIONAL CONFIGURATIONS
# =====================================================

# Stripe Configuration (Optional - for payment processing)
VITE_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key
STRIPE_SECRET_KEY=your_stripe_secret_key
